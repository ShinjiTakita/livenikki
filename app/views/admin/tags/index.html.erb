<div class="container">
  <div class="row pb-5">
   <div class="col-2"></div>
   <h4 style="background-color: silver;">ジャンル一覧・追加</h4>
  </div>
  <!--ジャンル追加機能-->
  <%= form_with model: @tag, url: admin_tags_path,local:true do |f| %>

  <div class="row pb-5">
   <div class="col-3">タグ名</div>
   <div class="col-4"><%= f.text_field :name %></div>
   <div class="col-4"><%= f.submit '新規登録', class: "btn btn-success" %></div>
  </div>
  <% end %>

  <!--ジャンル一覧表示-->
  <div class="row pb-5">
   <div class="col-"></div>
   <h4 class="border", style="background-color: silver;">タグ</h4>
  </div>
  <% @tags.each do |tag| %>
  <!--注意！！＝イコールはつけない！、each分には=不要-->

  <div class="row">
   <div class="col-3 border-top border-bottom pb-3"><%= tag.name %></div>
   <div class="col-3 border-bottom"><%= link_to '編集する', edit_admin_tag_path(tag.id), class: "btn btn-success" %></div>
  </div>
  <% end %>
</div>